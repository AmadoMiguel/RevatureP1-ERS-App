version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto8
  build:
    commands:
      - mvn test 
  post_build:
    commands:
      - mvn package
artifacts:
  files:
    - target/ers-api-1.0-SNAPSHOT.jar
    - appspec.yml
  discard-paths: yes